using UnityEngine;

public class BuildClusterRef : MonoBehaviour
{
    public Transform anchor;   // runtime link to anchor transform
    // optional persisted anchorGuid may be stored in BuildRecord (we don't store here)
}
